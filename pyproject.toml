[project]
name = "lawgraph-demo"
version = "0.2.0"
description = "Portfolio demo: autonomous product-law graphs with fail-closed gates and hash audit"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Martial Systems LLC" }]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
